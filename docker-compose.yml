services:
  postgres:
    image: postgres:16-alpine
    container_name: atjewel_postgres
    command: postgres -c timezone=America/Sao_Paulo
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      TZ: America/Sao_Paulo
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./src/shared/database/migrations/01_init.sql:/docker-entrypoint-initdb.d/01_init.sql
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}']
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - atjewel_network

  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: atjewel_api
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      SAFIRA_API_KEY: ${SAFIRA_API_KEY}
      PORT: 3000
      TZ: America/Sao_Paulo
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
    command: npm run start:dev
    networks:
      - atjewel_network

  # mongodb:
  #   image: mongo:7
  #   container_name: atjewel_mongo
  #   volumes:
  #     - mongo_data:/data/db
  #   networks:
  #     - atjewel_network
  #   # deferido para US10/US11 (agentes IA)

volumes:
  postgres_data:
  # mongo_data:

networks:
  atjewel_network:
    external: true
